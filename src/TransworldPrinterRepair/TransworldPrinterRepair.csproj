<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <AssemblyName>TransworldPrinterRepair</AssemblyName>
    <RootNamespace>Transworld.PrinterRepair</RootNamespace>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <ApplicationIcon>Resources\app.ico</ApplicationIcon>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>

    <!-- Solo Windows x64. -->
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <PlatformTarget>x64</PlatformTarget>
    <SelfContained>true</SelfContained>

    <!-- Ejecutable portable de un solo archivo, sin runtime previo. WPF no admite trimming. -->
    <PublishSingleFile>true</PublishSingleFile>
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
    <EnableCompressionInSingleFile>true</EnableCompressionInSingleFile>
    <PublishTrimmed>false</PublishTrimmed>
    <DebugType>none</DebugType>

    <Company>Transworld</Company>
    <Product>Reparacion de Impresora Transworld</Product>
    <AssemblyTitle>Reparacion de Impresora Transworld</AssemblyTitle>
    <Version>1.0.0</Version>
    <SatelliteResourceLanguages>en</SatelliteResourceLanguages>
  </PropertyGroup>

  <ItemGroup>
    <EmbeddedResource Include="Resources\areas.json" LogicalName="areas.json" />
    <EmbeddedResource Include="Resources\Drivers\*.zip" />

    <!-- El logo va como Resource de WPF para poder referenciarlo con una URI pack.
         Sigue funcionando con PublishSingleFile: queda dentro del ensamblado. -->
    <Resource Include="Resources\logo.png" />
  </ItemGroup>

</Project>
