<UserControl x:Class="Transworld.PrinterRepair.Presentation.Views.RepairConfirmationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Style="{StaticResource PageRoot}" Margin="40,28,40,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" Style="{StaticResource TitleText}" />

        <StackPanel Grid.Row="1" Margin="0,24,0,0" MaxWidth="640" HorizontalAlignment="Left">

            <Border Style="{StaticResource Card}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="ÁREA" Style="{StaticResource LabelText}" />
                        <TextBlock Text="{Binding AreaName}" Style="{StaticResource ValueText}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="IMPRESORA" Style="{StaticResource LabelText}" />
                        <TextBlock Text="{Binding PrinterName}" Style="{StaticResource ValueText}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="DIRECCIÓN IP" Style="{StaticResource LabelText}" />
                        <TextBlock Text="{Binding Ip}" Style="{StaticResource ValueText}" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Style="{StaticResource NoticeCard}" Margin="0,18,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⚠" FontSize="16" Foreground="{StaticResource DangerBrush}"
                               VerticalAlignment="Top" Margin="0,1,12,0" />
                    <TextBlock Text="{Binding Warning}" MaxWidth="540"
                               FontSize="13" TextWrapping="Wrap"
                               Foreground="{StaticResource TextPrimaryBrush}" />
                </StackPanel>
            </Border>

            <TextBlock Margin="0,16,0,0" FontSize="12" TextWrapping="Wrap"
                       Foreground="{StaticResource TextMutedBrush}"
                       Text="Windows te pedirá autorización para realizar los cambios. Acéptala para continuar." />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Command="{Binding CancelCommand}"
                    Style="{StaticResource SecondaryButton}" Margin="0,0,12,0" />
            <Button Content="Reparar impresora" Command="{Binding RepairCommand}"
                    Style="{StaticResource PrimaryButton}" IsDefault="True" />
        </StackPanel>
    </Grid>
</UserControl>
