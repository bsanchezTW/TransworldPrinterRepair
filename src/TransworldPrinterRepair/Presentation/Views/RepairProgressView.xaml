<UserControl x:Class="Transworld.PrinterRepair.Presentation.Views.RepairProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Style="{StaticResource PageRoot}" Margin="40,28,40,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding Title}" Style="{StaticResource TitleText}" />
            <TextBlock Style="{StaticResource SubtitleText}">
                <Run Text="Área:" />
                <Run Text="{Binding AreaName, Mode=OneWay}" FontWeight="SemiBold" />
            </TextBlock>
        </StackPanel>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Stages}" Margin="0,30,0,0"
                      VerticalAlignment="Top">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,4" Height="46">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="34" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock x:Name="Glyph" Grid.Column="0" Text="{Binding Glyph}"
                                   FontSize="17" VerticalAlignment="Center"
                                   Foreground="{StaticResource TextMutedBrush}" />

                        <TextBlock x:Name="Label" Grid.Column="1" Text="{Binding Title}"
                                   FontSize="16" VerticalAlignment="Center"
                                   Foreground="{StaticResource TextMutedBrush}" />
                    </Grid>

                    <!-- El color y el peso siguen el estado REAL informado por el worker. -->
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding State}" Value="Running">
                            <Setter TargetName="Glyph" Property="Foreground" Value="{StaticResource AccentBrush}" />
                            <Setter TargetName="Label" Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                            <Setter TargetName="Label" Property="FontWeight" Value="SemiBold" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding State}" Value="Ok">
                            <Setter TargetName="Glyph" Property="Foreground" Value="{StaticResource SuccessBrush}" />
                            <Setter TargetName="Label" Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding State}" Value="Warning">
                            <Setter TargetName="Glyph" Property="Foreground" Value="{StaticResource WarningBrush}" />
                            <Setter TargetName="Label" Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding State}" Value="Failed">
                            <Setter TargetName="Glyph" Property="Foreground" Value="{StaticResource DangerBrush}" />
                            <Setter TargetName="Label" Property="Foreground" Value="{StaticResource DangerBrush}" />
                            <Setter TargetName="Label" Property="FontWeight" Value="SemiBold" />
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                CornerRadius="10" Padding="16,12">
            <TextBlock Text="{Binding Detail}" FontSize="13"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       TextTrimming="CharacterEllipsis" />
        </Border>
    </Grid>
</UserControl>
