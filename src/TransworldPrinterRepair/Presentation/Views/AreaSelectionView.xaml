<UserControl x:Class="Transworld.PrinterRepair.Presentation.Views.AreaSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Style="{StaticResource PageRoot}" Margin="40,28,40,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding Title}" Style="{StaticResource TitleText}" />
            <TextBlock Text="{Binding Subtitle}" Style="{StaticResource SubtitleText}"
                       MaxWidth="620" HorizontalAlignment="Left" />
        </StackPanel>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Areas}" Margin="0,26,0,0"
                      KeyboardNavigation.TabNavigation="Cycle">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="2" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding SelectCommand}"
                            CommandParameter="{Binding Area}"
                            Style="{StaticResource CardButton}"
                            AutomationProperties.Name="{Binding Name}"
                            Margin="0,0,14,14">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Name}"
                                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />

                            <!-- Marca el area usada la ultima vez en este equipo. -->
                            <Border Grid.Column="1" Visibility="Collapsed" x:Name="LastUsedTag"
                                    Background="{StaticResource AccentBrush}" CornerRadius="9"
                                    Padding="9,3" VerticalAlignment="Center">
                                <TextBlock Text="Tu área" Foreground="White"
                                           FontSize="11" FontWeight="SemiBold" />
                            </Border>
                        </Grid>
                    </Button>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsLastUsed}" Value="True">
                            <Setter TargetName="LastUsedTag" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Grid.Row="2" Content="⚙"
                Command="{Binding OpenAdminCommand}"
                Style="{StaticResource IconButton}"
                HorizontalAlignment="Right"
                ToolTip="Configuración administrativa (requiere permisos de Windows)" />
    </Grid>
</UserControl>
